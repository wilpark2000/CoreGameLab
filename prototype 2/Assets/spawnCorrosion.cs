using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class spawnCorrosion : MonoBehaviour
{
    public GameObject corrosion;
    // Start is called before the first frame update
    void Start()
    {
        //for (int i = 0; i < 10; i++)
        //{
        //    float xpos = Random.Range(-30, 55);
        //    float ypos = Random.Range(-22, 26);
        //    Vector2 direction = new Vector2(Random.Range(-5.0f, 5.0f), Random.Range(-5.0f, 5.0f));
        //    corrosion = Instantiate(corrosion, corrosion.transform.position = new Vector3(xpos, ypos, -5), transform.rotation) as GameObject;
        //    corrosion.GetComponent<Rigidbody2D>().AddForce(direction * 20f);
        //}
    }

    // Update is called once per frame
    void Update()
    {
       
    }
}
